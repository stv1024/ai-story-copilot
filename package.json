{
  "name": "ai-story-copilot",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm --workspace @ai-story-copilot/web run dev",
    "start": "npm --workspace @ai-story-copilot/web run dev",
    "build": "npm --workspace @ai-story-copilot/web run build",
    "test": "node --test"
  }
}
