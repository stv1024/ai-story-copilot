# apps/api

Reserved for future Node.js or serverless endpoints.

Current web app calls OpenRouter directly from the browser, but the frontend service layer is intentionally isolated so this folder can later host a proxy or orchestration layer without rewriting the UI.

